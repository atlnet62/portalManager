import image from "./../../../assets/images/spinner.svg";

function Loading() {
    return (
        <main id="loading">
            <img src={image} alt="Loading..." />
        </main>
    );
}

export default Loading;
